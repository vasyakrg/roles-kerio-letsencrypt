lets_deploy_certpath: "/opt/kerio/mailserver/sslcert"
lets_deploy_letsencrypt_email: test@mail.ru
lets_dry_run: "--dry-run"
lets_monthly: "monthly"
lets_crontab_enable: "enable"

lets_deploy_vhosts:
  - server_name: test.local
    state: present
